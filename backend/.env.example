DATABASE_URL=postgres://postgres:postgres@localhost:5432/leaderboard
REDIS_URL=redis://localhost:6379
PORT=4000
CACHE_TTL_SECONDS=60
# rows seeded into orders so the GROUP BY is genuinely slow
SEED_ORDERS=50000
